# Secrets / env
.env
.env.*
!.env.example
/config/master.key
/config/credentials/*.key

# Rails
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
/storage/*
!/storage/.keep
/db/*.sqlite3*
/public/assets
/public/vite

# Ruby
/.bundle
/vendor/bundle

# Node / frontend
node_modules/
frontend/dist/
frontend/coverage/
*.local

# Editors / OS
.DS_Store
Thumbs.db
.idea/
*.swp

# Test artifacts
/coverage
/e2e/test-results/
/e2e/playwright-report/

# Ignore key files for decrypting credentials and more.
/config/*.key


# Claude harness artifacts
.claude/scheduled_tasks.lock

# Local ruflo/ruvector plugin state
ruvector.db

/portfolioExample

# Agent worktrees created by Agent(..., isolation: "worktree") — local state,
# and embedded git repos that must never be committed into the outer repo.
/.claude/worktrees/
